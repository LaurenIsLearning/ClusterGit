{
    "sourceFile": "backend/src/config/config.js",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 8,
            "patches": [
                {
                    "date": 1772955084591,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1772955090905,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,26 @@\n+import path from 'path';\r\n+import os from 'os';\r\n+\r\n+// Repository storage configuration\r\n+const rawRepoPath = process.env.REPO_BASE_PATH || // path.join(os.homedir(), 'clustergit-repos');\r\n+export const REPO_BASE_PATH = path.isAbsolute(rawRepoPath)\r\n+    ? rawRepoPath\r\n+    : path.resolve(process.cwd(), rawRepoPath);\r\n+\r\n+// Git-annex configuration\r\n+export const GIT_ANNEX_CONFIG = {\r\n+    backend: 'SHA256E', // Cryptographic hash + extension\r\n+    largeFileThreshold: 1024 * 1024, // 1MB\r\n+    numCopies: 1, // Minimum number of copies\r\n+};\r\n+\r\n+// Server configuration\r\n+export const SERVER_HOST = process.env.SERVER_HOST || 'localhost';\r\n+export const SERVER_PORT = process.env.PORT || 8080;\r\n+\r\n+export default {\r\n+    REPO_BASE_PATH,\r\n+    GIT_ANNEX_CONFIG,\r\n+    SERVER_HOST,\r\n+    SERVER_PORT,\r\n+};\r\n"
                },
                {
                    "date": 1772955096521,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,26 @@\n+import path from 'path';\r\n+import os from 'os';\r\n+\r\n+// Repository storage configuration\r\n+const rawRepoPath = process.env.REPO_BASE_PATH || // path.join(os.homedir(), 'clustergit-repos');    T\r\n+export const REPO_BASE_PATH = path.isAbsolute(rawRepoPath)\r\n+    ? rawRepoPath\r\n+    : path.resolve(process.cwd(), rawRepoPath);\r\n+\r\n+// Git-annex configuration\r\n+export const GIT_ANNEX_CONFIG = {\r\n+    backend: 'SHA256E', // Cryptographic hash + extension\r\n+    largeFileThreshold: 1024 * 1024, // 1MB\r\n+    numCopies: 1, // Minimum number of copies\r\n+};\r\n+\r\n+// Server configuration\r\n+export const SERVER_HOST = process.env.SERVER_HOST || 'localhost';\r\n+export const SERVER_PORT = process.env.PORT || 8080;\r\n+\r\n+export default {\r\n+    REPO_BASE_PATH,\r\n+    GIT_ANNEX_CONFIG,\r\n+    SERVER_HOST,\r\n+    SERVER_PORT,\r\n+};\r\n"
                },
                {
                    "date": 1772955103162,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,26 @@\n+import path from 'path';\r\n+import os from 'os';\r\n+\r\n+// Repository storage configuration\r\n+const rawRepoPath = process.env.REPO_BASE_PATH || // path.join(os.homedir(), 'clustergit-repos');    This gets seen as root/\r\n+export const REPO_BASE_PATH = path.isAbsolute(rawRepoPath)\r\n+    ? rawRepoPath\r\n+    : path.resolve(process.cwd(), rawRepoPath);\r\n+\r\n+// Git-annex configuration\r\n+export const GIT_ANNEX_CONFIG = {\r\n+    backend: 'SHA256E', // Cryptographic hash + extension\r\n+    largeFileThreshold: 1024 * 1024, // 1MB\r\n+    numCopies: 1, // Minimum number of copies\r\n+};\r\n+\r\n+// Server configuration\r\n+export const SERVER_HOST = process.env.SERVER_HOST || 'localhost';\r\n+export const SERVER_PORT = process.env.PORT || 8080;\r\n+\r\n+export default {\r\n+    REPO_BASE_PATH,\r\n+    GIT_ANNEX_CONFIG,\r\n+    SERVER_HOST,\r\n+    SERVER_PORT,\r\n+};\r\n"
                },
                {
                    "date": 1772955124006,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,9 +1,9 @@\n import path from 'path';\r\n import os from 'os';\r\n \r\n // Repository storage configuration\r\n-const rawRepoPath = process.env.REPO_BASE_PATH || // path.join(os.homedir(), 'clustergit-repos');    This gets seen as root/\r\n+const rawRepoPath = process.env.REPO_BASE_PATH || // path.join(os.homedir(), 'clustergit-repos');    This gets seen as root/clustr\r\n export const REPO_BASE_PATH = path.isAbsolute(rawRepoPath)\r\n     ? rawRepoPath\r\n     : path.resolve(process.cwd(), rawRepoPath);\r\n \r\n"
                },
                {
                    "date": 1772955136963,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,9 +1,9 @@\n import path from 'path';\r\n import os from 'os';\r\n \r\n // Repository storage configuration\r\n-const rawRepoPath = process.env.REPO_BASE_PATH || // path.join(os.homedir(), 'clustergit-repos');    This gets seen as root/clustr\r\n+const rawRepoPath = process.env.REPO_BASE_PATH || // path.join(os.homedir(), 'clustergit-repos');    This gets seen as root/clust\r\n export const REPO_BASE_PATH = path.isAbsolute(rawRepoPath)\r\n     ? rawRepoPath\r\n     : path.resolve(process.cwd(), rawRepoPath);\r\n \r\n"
                },
                {
                    "date": 1772955153987,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,26 @@\n+import path from 'path';\r\n+import os from 'os';\r\n+\r\n+// Repository storage configuration\r\n+const rawRepoPath = process.env.REPO_BASE_PATH || // path.join(os.homedir(), 'clustergit-repos');    This gets seen as root/clustergit-repos in docker\r\n+export const REPO_BASE_PATH = path.isAbsolute(rawRepoPath)\r\n+    ? rawRepoPath\r\n+    : path.resolve(process.cwd(), rawRepoPath);\r\n+\r\n+// Git-annex configuration\r\n+export const GIT_ANNEX_CONFIG = {\r\n+    backend: 'SHA256E', // Cryptographic hash + extension\r\n+    largeFileThreshold: 1024 * 1024, // 1MB\r\n+    numCopies: 1, // Minimum number of copies\r\n+};\r\n+\r\n+// Server configuration\r\n+export const SERVER_HOST = process.env.SERVER_HOST || 'localhost';\r\n+export const SERVER_PORT = process.env.PORT || 8080;\r\n+\r\n+export default {\r\n+    REPO_BASE_PATH,\r\n+    GIT_ANNEX_CONFIG,\r\n+    SERVER_HOST,\r\n+    SERVER_PORT,\r\n+};\r\n"
                },
                {
                    "date": 1772955159947,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,26 @@\n+import path from 'path';\r\n+import os from 'os';\r\n+\r\n+// Repository storage configuration\r\n+const rawRepoPath = process.env.REPO_BASE_PATH || // path.join(os.homedir(), 'clustergit-repos');    This gets seen as root/clustergit-repos in docker, not=====\r\n+export const REPO_BASE_PATH = path.isAbsolute(rawRepoPath)\r\n+    ? rawRepoPath\r\n+    : path.resolve(process.cwd(), rawRepoPath);\r\n+\r\n+// Git-annex configuration\r\n+export const GIT_ANNEX_CONFIG = {\r\n+    backend: 'SHA256E', // Cryptographic hash + extension\r\n+    largeFileThreshold: 1024 * 1024, // 1MB\r\n+    numCopies: 1, // Minimum number of copies\r\n+};\r\n+\r\n+// Server configuration\r\n+export const SERVER_HOST = process.env.SERVER_HOST || 'localhost';\r\n+export const SERVER_PORT = process.env.PORT || 8080;\r\n+\r\n+export default {\r\n+    REPO_BASE_PATH,\r\n+    GIT_ANNEX_CONFIG,\r\n+    SERVER_HOST,\r\n+    SERVER_PORT,\r\n+};\r\n"
                },
                {
                    "date": 1772955174575,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,9 +1,9 @@\n import path from 'path';\r\n import os from 'os';\r\n \r\n // Repository storage configuration\r\n-const rawRepoPath = process.env.REPO_BASE_PATH || // path.join(os.homedir(), 'clustergit-repos');    This gets seen as root/clustergit-repos in docker, not=====\r\n+const rawRepoPath = process.env.REPO_BASE_PATH || \"/repos\" // path.join(os.homedir(), 'clustergit-repos');    This gets seen as root/clustergit-repos in docker\r\n export const REPO_BASE_PATH = path.isAbsolute(rawRepoPath)\r\n     ? rawRepoPath\r\n     : path.resolve(process.cwd(), rawRepoPath);\r\n \r\n@@ -23,134 +23,4 @@\n     GIT_ANNEX_CONFIG,\r\n     SERVER_HOST,\r\n     SERVER_PORT,\r\n };\r\n-import path from 'path';\r\n-import os from 'os';\r\n-\r\n-// Repository storage configuration\r\n-const rawRepoPath = process.env.REPO_BASE_PATH || // path.join(os.homedir(), 'clustergit-repos');    This gets seen as root/clustergit-repos in docker\r\n-export const REPO_BASE_PATH = path.isAbsolute(rawRepoPath)\r\n-    ? rawRepoPath\r\n-    : path.resolve(process.cwd(), rawRepoPath);\r\n-\r\n-// Git-annex configuration\r\n-export const GIT_ANNEX_CONFIG = {\r\n-    backend: 'SHA256E', // Cryptographic hash + extension\r\n-    largeFileThreshold: 1024 * 1024, // 1MB\r\n-    numCopies: 1, // Minimum number of copies\r\n-};\r\n-\r\n-// Server configuration\r\n-export const SERVER_HOST = process.env.SERVER_HOST || 'localhost';\r\n-export const SERVER_PORT = process.env.PORT || 8080;\r\n-\r\n-export default {\r\n-    REPO_BASE_PATH,\r\n-    GIT_ANNEX_CONFIG,\r\n-    SERVER_HOST,\r\n-    SERVER_PORT,\r\n-};\r\n-import path from 'path';\r\n-import os from 'os';\r\n-\r\n-// Repository storage configuration\r\n-const rawRepoPath = process.env.REPO_BASE_PATH || // path.join(os.homedir(), 'clustergit-repos');    This gets seen as root/clust\r\n-export const REPO_BASE_PATH = path.isAbsolute(rawRepoPath)\r\n-    ? rawRepoPath\r\n-    : path.resolve(process.cwd(), rawRepoPath);\r\n-\r\n-// Git-annex configuration\r\n-export const GIT_ANNEX_CONFIG = {\r\n-    backend: 'SHA256E', // Cryptographic hash + extension\r\n-    largeFileThreshold: 1024 * 1024, // 1MB\r\n-    numCopies: 1, // Minimum number of copies\r\n-};\r\n-\r\n-// Server configuration\r\n-export const SERVER_HOST = process.env.SERVER_HOST || 'localhost';\r\n-export const SERVER_PORT = process.env.PORT || 8080;\r\n-\r\n-export default {\r\n-    REPO_BASE_PATH,\r\n-    GIT_ANNEX_CONFIG,\r\n-    SERVER_HOST,\r\n-    SERVER_PORT,\r\n-};\r\n-import path from 'path';\r\n-import os from 'os';\r\n-\r\n-// Repository storage configuration\r\n-const rawRepoPath = process.env.REPO_BASE_PATH || // path.join(os.homedir(), 'clustergit-repos');    T\r\n-export const REPO_BASE_PATH = path.isAbsolute(rawRepoPath)\r\n-    ? rawRepoPath\r\n-    : path.resolve(process.cwd(), rawRepoPath);\r\n-\r\n-// Git-annex configuration\r\n-export const GIT_ANNEX_CONFIG = {\r\n-    backend: 'SHA256E', // Cryptographic hash + extension\r\n-    largeFileThreshold: 1024 * 1024, // 1MB\r\n-    numCopies: 1, // Minimum number of copies\r\n-};\r\n-\r\n-// Server configuration\r\n-export const SERVER_HOST = process.env.SERVER_HOST || 'localhost';\r\n-export const SERVER_PORT = process.env.PORT || 8080;\r\n-\r\n-export default {\r\n-    REPO_BASE_PATH,\r\n-    GIT_ANNEX_CONFIG,\r\n-    SERVER_HOST,\r\n-    SERVER_PORT,\r\n-};\r\n-import path from 'path';\r\n-import os from 'os';\r\n-\r\n-// Repository storage configuration\r\n-const rawRepoPath = process.env.REPO_BASE_PATH || // path.join(os.homedir(), 'clustergit-repos');\r\n-export const REPO_BASE_PATH = path.isAbsolute(rawRepoPath)\r\n-    ? rawRepoPath\r\n-    : path.resolve(process.cwd(), rawRepoPath);\r\n-\r\n-// Git-annex configuration\r\n-export const GIT_ANNEX_CONFIG = {\r\n-    backend: 'SHA256E', // Cryptographic hash + extension\r\n-    largeFileThreshold: 1024 * 1024, // 1MB\r\n-    numCopies: 1, // Minimum number of copies\r\n-};\r\n-\r\n-// Server configuration\r\n-export const SERVER_HOST = process.env.SERVER_HOST || 'localhost';\r\n-export const SERVER_PORT = process.env.PORT || 8080;\r\n-\r\n-export default {\r\n-    REPO_BASE_PATH,\r\n-    GIT_ANNEX_CONFIG,\r\n-    SERVER_HOST,\r\n-    SERVER_PORT,\r\n-};\r\n-import path from 'path';\r\n-import os from 'os';\r\n-\r\n-// Repository storage configuration\r\n-const rawRepoPath = process.env.REPO_BASE_PATH || path.join(os.homedir(), 'clustergit-repos');\r\n-export const REPO_BASE_PATH = path.isAbsolute(rawRepoPath)\r\n-    ? rawRepoPath\r\n-    : path.resolve(process.cwd(), rawRepoPath);\r\n-\r\n-// Git-annex configuration\r\n-export const GIT_ANNEX_CONFIG = {\r\n-    backend: 'SHA256E', // Cryptographic hash + extension\r\n-    largeFileThreshold: 1024 * 1024, // 1MB\r\n-    numCopies: 1, // Minimum number of copies\r\n-};\r\n-\r\n-// Server configuration\r\n-export const SERVER_HOST = process.env.SERVER_HOST || 'localhost';\r\n-export const SERVER_PORT = process.env.PORT || 8080;\r\n-\r\n-export default {\r\n-    REPO_BASE_PATH,\r\n-    GIT_ANNEX_CONFIG,\r\n-    SERVER_HOST,\r\n-    SERVER_PORT,\r\n-};\r\n"
                }
            ],
            "date": 1772955084591,
            "name": "Commit-0",
            "content": "import path from 'path';\r\nimport os from 'os';\r\n\r\n// Repository storage configuration\r\nconst rawRepoPath = process.env.REPO_BASE_PATH || path.join(os.homedir(), 'clustergit-repos');\r\nexport const REPO_BASE_PATH = path.isAbsolute(rawRepoPath)\r\n    ? rawRepoPath\r\n    : path.resolve(process.cwd(), rawRepoPath);\r\n\r\n// Git-annex configuration\r\nexport const GIT_ANNEX_CONFIG = {\r\n    backend: 'SHA256E', // Cryptographic hash + extension\r\n    largeFileThreshold: 1024 * 1024, // 1MB\r\n    numCopies: 1, // Minimum number of copies\r\n};\r\n\r\n// Server configuration\r\nexport const SERVER_HOST = process.env.SERVER_HOST || 'localhost';\r\nexport const SERVER_PORT = process.env.PORT || 8080;\r\n\r\nexport default {\r\n    REPO_BASE_PATH,\r\n    GIT_ANNEX_CONFIG,\r\n    SERVER_HOST,\r\n    SERVER_PORT,\r\n};\r\n"
        }
    ]
}